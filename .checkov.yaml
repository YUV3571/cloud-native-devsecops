skip-check:
  # Registry modules are version-pinned and maintained upstream; this repository
  # intentionally uses the public Terraform Registry instead of vendoring forks.
  - CKV_TF_1

  # Lambda code signing requires a full signer pipeline and trusted publisher
  # workflow. The function source is generated locally and pinned by hash in CI,
  # so this remains a documented follow-up rather than a partial placeholder.
  - CKV_AWS_272
